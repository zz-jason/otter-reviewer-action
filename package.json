{
  "name": "otter-reviewer-action",
  "version": "1.0.1",
  "description": "GitHub Action that runs an agent CLI on a self-hosted runner and posts PR inline review comments as a GitHub App.",
  "bin": {
    "otter-reviewer": "bin/otter-reviewer.js"
  },
  "scripts": {
    "check": "node --check bin/otter-reviewer.js && node --check tests/filter.test.js",
    "test": "node tests/filter.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
